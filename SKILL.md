---
name: jeremy-design
description: 以 Apple 设计理念为默认基础，设计、实现和改进 Web、移动界面与企业工作台；融入紧凑数据界面、轻质面板与清晰模块入口的实践。用于页面布局、视觉层级、组件状态与交互落地，无需额外声明 Apple 风格；纯后端、无视觉影响的代码修改不触发。
---

# Jeremy Design

以 Apple 设计理念为共同基础，把用户任务组织成清晰、轻盈、可操作的界面。工作台的紧凑排版、模块入口和数据面板是同一设计语言在复杂业务中的延伸，不是另一套企业风格。具体 HIG 平台数值与 Web 适配仍按来源区分。

## 先判断，再设计

从现有页面、`DESIGN.md`、组件和用户参考中确定：主要使用者、第一任务、内容形态、输入方式、已有视觉系统。能读到的信息直接读取；只有会改变方案的缺口才问用户。

先用一小段说明本次选择：页面类型、主要操作、布局结构、密度和保留的设计特征，然后实施。局部修复不要求额外方案文档。

优先级：用户明确要求 → 项目设计规范与已选参考 → 本次产品模式 → 本 skill 默认值。规范与现有代码不一致时区分“目标规范”和“观察到的实现”，不要静默把其中一方当成另一方。

**默认先读 [Apple Web 设计基础](references/apple-web-foundation.md)**，再按下表补充布局和交互资料。用户只说“工作台”“后台”或“重构”也采用这套基础，无需再次说 Apple；已有品牌或明确风格要求优先，局部修复不擅自全站换肤。

| 当前任务 | 读取 |
|---|---|
| 企业门户、应用首页、数据管理界面 | [工作台交互](patterns/workbench.md) + [Apple 风格工作台元素](references/enterprise-visual-language.md)，延续共同基础 |
| 移动 Web、小屏触控界面 | [移动 Web](patterns/mobile-web.md) + [Web 适配](references/web-adaptation.md) |
| 网站、内容展示、新建页面方向未定 | [设计流程](references/design-workflow.md) |
| 需要 Apple 平台的具体规范、控件或原生参数 | [Apple 速查](references/apple-quick-reference.md)，再按平台和组件查对应资料；这不是启用 Apple 设计基础的前提 |
| 建立或补充设计 token | [Web tokens](references/web-tokens.md)；需要企业界面视觉配方时参考其成对色值 |
| 列表、表单、保存、异步任务 | [状态与表单](references/states-and-forms.md) |
| React 实现 | [React 约定](references/react-implementation.md)，仅在项目使用 React 时读取 |
| 布局改造、响应式问题、视觉验收 | [按影响范围验证](references/detail-audit.md) |

每次只加载相关资料。不要为一个桌面表格任务读取整套 Apple 平台手册或引入新的 UI 框架。

## 现有界面改版：先留基线，再扩展

整页改版、替换全局 CSS 或重做公共组件时，动手前读取 [改版检查](references/redesign-checks.md)；局部调整只检查受影响的组件和状态。

1. **留基线。** 在可运行环境中先打开原版，保存代表页面和关键状态截图；记录角色、数据、窗口和缩放，供同条件比较。
2. **查兼容。** 核对静态 HTML、动态生成 DOM、CSS 选择器与 JS 显隐状态的对应关系。保留 ID、类名或 JS 不等于功能无损；不能用默认隐藏提示来实现降噪。
3. **先验证样板。** 先完成有代表性的内容页与复杂组件，比较层级、密度和操作可达性，再推广公共样式；不能只凭登录页或首页决定全站通过。
4. **检查状态变化。** 全局改版覆盖已有的告警、Toast 显示与消失、弹窗焦点，以及断点前后的跨模块导航。缺少某类组件时不为验收新增它。
5. **据证据交付。** 实际查看同状态前后截图，并分别记录视觉与交互结果；确认内容消失、局部溢出或操作不可达时先修复。运行环境受限可继续安全的代码工作，但明确尚未完成视觉验收。

## 实施原则

- **布局服务内容。** 根据浏览、比较、输入或编辑的主要任务选择网格、列表、表单和主辅分区，不预设固定业务页面或流程。
- **既有项目复用优先。** 延续真实业务标签、路由、组件和数据契约。局部修复只改相关部分；新建或明确改版才重建视觉系统。
- **精致来自层次、对齐和节奏。** 工作台用紧凑标题、清晰表头、统一数字与少量语义强调；不要靠大标题、装饰性统计卡和全屏模糊制造“高级感”。现有项目有良好材质时保留。
- **信息密度按输入方式决定。** 桌面控件可以紧凑；触控扩大命中区域。Apple point 不直接换算为 CSS px。Web 无障碍口径见 [无障碍](references/accessibility.md)。
- **数据与状态真实。** 未加载用占位，未知值不写成 0；演示数据明确标记。按钮成功态依据实际结果。不要为了演示触发真实业务写入。
- **任务闭环可见。** 当前范围、处理状态、阻塞原因和可执行下一步同屏可得。异步任务失败后保留输入和恢复入口，部分失败不包装为全部成功。
- **主题和动效按需。** 浅色工作台不强行新增暗色模式；手势、弹簧、玻璃材质仅在适合的界面采用。复用现有图标体系，不为更换图标引入依赖。

## 验证与交付

**每次完成视觉实现，都执行 [成品自检](references/finished-ui-check.md)，新建和改版相同，不需要旧版作参照。** 默认闭环是：打开实际页面 → 从真实入口触发状态 → 检测并查看截图 → 修复问题 → 重复原操作复测 → 交付。局部修改只覆盖受影响组件，全局修改覆盖代表页面类型。

不要只截常态首页：检查已有 Toast、菜单、弹窗的出现、布局和关闭，以及长内容、窄窗口与放大后的操作可达性。特别检查图标、文案、关闭按钮是否散落或重叠；消息消失后是否仍占位或挡住操作。可用 `scripts/ui-self-check.js` 在当前浏览器记录可见性、容器边界与焦点检查；几何检测、交互结果和目视结论分别记录为通过、失败、未执行。

发现可读性或操作缺陷就修复并复测，不能以零报错、零页面溢出结束。未运行浏览器或未查看截图，明确“界面尚未完成验证”。改版另加前后比较；档位与几何工具见 [细节审查](references/detail-audit.md)。

检测器只辅助发现问题；脚本零阻断不证明视觉好看或业务正确。不要为了消除启发式警告破坏正常布局。说明改动、验证证据与未验证范围，不把未运行的检查写成通过。

## 来源与进阶参考

- `PROJECT-DEFAULT`：本 skill 的可调整默认值；`WEB-ADAPTATION`：浏览器实现建议。
- `HIG`：Apple 官方指南；`APPLE-EXAMPLE`：特定演讲/示例。保留适用平台和条件，不能升级成所有网站的通用要求。
- Apple 参考库保留原仓库标注的 2026-09 快照；本次改造不宣称重新核验全部 HIG 数值。需精确官方依据时查原文。

按需查阅：[布局](references/layout.md)、[字体](references/typography.md)、[颜色](references/color-dark-mode.md)、[材质](references/materials.md)、[控件](references/components-controls.md)、[图标](references/icons-symbols.md)、[交互反馈](references/interaction-feedback.md)、[动效](references/motion.md)、[流体手势](references/fluid-motion-web.md)、[平台差异](references/platforms.md)、[设计原则](references/design-principles.md)、[HIG URL 索引](references/hig-index-all-pages.md)。
